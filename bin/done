#!/usr/bin/env ruby

$:.push File.expand_path("../../lib", __FILE__)
require 'done'
require 'done/cli'

begin
  Done::CLI.start(ARGV)
rescue Done::Config::InvalidConfig => e
  puts e.message
  exit 1
end
